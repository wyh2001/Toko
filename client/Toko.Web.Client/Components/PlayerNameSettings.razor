@inject Toko.Web.Client.Services.IPlayerNameService PlayerNameService
@rendermode @(new InteractiveWebAssemblyRenderMode(prerender: false))
<div class="player-name-settings">
    <div class="player-name-container">
        <div class="player-name-label">Playing as:</div>
        <div class="player-name-display" @onclick="ToggleEdit">
            <span class="player-name-value">@currentPlayerName</span>
            <button class="player-name-edit-btn" @onclick:stopPropagation="true" @onclick="ToggleEdit">▼</button>
        </div>
    </div>
    
    @if (isEditing)
    {
        <div class="player-name-edit">
            <input type="text" @bind="editPlayerName" @onkeypress="HandleKeyPress" 
                   placeholder="Enter your name" maxlength="50" class="player-name-input" />
            <div class="player-name-hint">Name changes take effect in new rooms</div>
            <button class="save-btn" @onclick="SavePlayerName">Save</button>
            <button class="cancel-btn" @onclick="CancelEdit">Cancel</button>
        </div>
    }
</div>

@code {
    private string currentPlayerName = "";
    private string editPlayerName = "";
    private bool isEditing = false;

    protected override async Task OnInitializedAsync()
    {
        currentPlayerName = await PlayerNameService.GetPlayerNameAsync();
        editPlayerName = currentPlayerName;
    }

    private void ToggleEdit()
    {
        if (isEditing)
        {
            CancelEdit();
        }
        else
        {
            editPlayerName = currentPlayerName;
            isEditing = true;
        }
    }

    private async Task SavePlayerName()
    {
        if (!string.IsNullOrWhiteSpace(editPlayerName))
        {
            await PlayerNameService.SetPlayerNameAsync(editPlayerName.Trim());
            currentPlayerName = editPlayerName.Trim();
        }
        isEditing = false;
    }

    private void CancelEdit()
    {
        editPlayerName = currentPlayerName;
        isEditing = false;
    }

    private async Task HandleKeyPress(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await SavePlayerName();
        }
        else if (e.Key == "Escape")
        {
            CancelEdit();
        }
    }
}

<style>
    .player-name-settings {
        display: inline-block;
        position: relative;
    }

    .player-name-container {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .player-name-hint {
        font-size: 0.6rem;
        color: #888888;
        margin: 0.5rem 0;
        font-style: italic;
        text-align: center;
        line-height: 1.2;
    }

    .player-name-label {
        font-size: 0.7rem;
        color: #666666;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        white-space: nowrap;
    }

    .player-name-display {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1rem;
        background: #ffffff;
        border: 2px solid #000000;
        transition: all 0.15s ease;
        cursor: pointer;
        font-family: 'Inter', sans-serif;
    }

    .player-name-display:hover {
        background: #f5f5f5;
        transform: translate(-1px, -1px);
        box-shadow: 2px 2px 0 #000000;
    }

    .player-name-display:active {
        transform: translate(0, 0);
        box-shadow: none;
    }

    .player-name-value {
        font-weight: 700;
        color: #000000;
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.02em;
    }

    .player-name-edit-btn {
        background: none;
        border: none;
        cursor: pointer;
        font-size: 0.8rem;
        opacity: 0.6;
        transition: all 0.15s ease;
        color: #000000;
        padding: 0;
    }

    .player-name-edit-btn:hover {
        opacity: 1;
    }

    .player-name-edit {
        position: absolute;
        top: 100%;
        right: 0;
        margin-top: 4px;
        display: flex;
        flex-direction: column;
        gap: 8px;
        background: #ffffff;
        padding: 1rem;
        border: 2px solid #000000;
        min-width: 200px;
        z-index: 1001;
    }

    .player-name-input {
        padding: 0.75rem;
        border: 2px solid #000000;
        background: #ffffff;
        font-size: 0.8rem;
        font-family: 'Inter', sans-serif;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.02em;
        transition: all 0.15s ease;
    }

    .player-name-input:focus {
        outline: none;
        background: #f5f5f5;
    }

    .save-btn, .cancel-btn {
        padding: 0.75rem 1rem;
        border: 2px solid #000000;
        font-size: 0.7rem;
        cursor: pointer;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        transition: all 0.15s ease;
        font-family: 'Inter', sans-serif;
    }

    .save-btn {
        background-color: #000000;
        color: #ffffff;
    }

    .save-btn:hover {
        background-color: #222222;
        transform: translate(-1px, -1px);
        box-shadow: 2px 2px 0 #666666;
    }

    .save-btn:active {
        transform: translate(0, 0);
        box-shadow: none;
    }

    .cancel-btn {
        background-color: #ffffff;
        color: #000000;
    }

    .cancel-btn:hover {
        background-color: #f5f5f5;
        transform: translate(-1px, -1px);
        box-shadow: 2px 2px 0 #000000;
    }

    .cancel-btn:active {
        transform: translate(0, 0);
        box-shadow: none;
    }
</style>